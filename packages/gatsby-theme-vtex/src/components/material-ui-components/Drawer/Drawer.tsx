import React, { FC } from 'react'
import MaterialDrawer, { DrawerProps } from '@material-ui/core/Drawer'

const Drawer: FC<DrawerProps> = (props) => <MaterialDrawer {...props} />

export default Drawer
