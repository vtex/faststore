import { Meta, Story, Preview, ArgsTable } from '@storybook/addon-docs/blocks'
import Icon from '.'
import ShoppingCart from './assets/ShoppingCart'

<Meta title="Atoms/Icon" />

# Preview

<Preview>
  <Story
    name="Icon"
    args={{
      style: {
        fontSize: '32px',
        color: 'red',
        display: 'inline-block',
        lineHeight: 0,
      },
    }}
  >
    {(args) => <Icon {...args} component={<ShoppingCart />} />}
  </Story>
</Preview>

# Props

<ArgsTable of={Icon} />
