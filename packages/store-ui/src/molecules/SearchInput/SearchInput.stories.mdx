import { Meta, Story, Preview, ArgsTable } from '@storybook/addon-docs/blocks'
import SearchInput from '.'
import CustomIcon from './assets/CustomIcon'

<Meta title="Molecules/SearchInput" />

# Default

<Preview>
  <Story
    name="Default"
    args={{
      placeholder: 'Search',
    }}
  >
    {({placeholder}) => (
      <SearchInput
        onSubmit={(value) => alert(`[search]: ${value}`)}
        placeholder={placeholder}
      />
    )}
  </Story>
</Preview>

# With custom icon

<Preview>
  <Story
    name="CustomIcon"
    args={{
      placeholder: 'Search',
    }}
  >
    {({placeholder}) => (
      <SearchInput
        icon={<CustomIcon />}
        onSubmit={(value) => alert(`[search]: ${value}`)}
        placeholder={placeholder}
      />
    )}
  </Story>
</Preview>

# Props

<ArgsTable of={SearchInput} />
