{
  "$extends": ["#/$defs/base-component"],
  "$componentKey": "AccountOrderDetails",
  "$componentTitle": "Account Order Details",
  "title": "Account Order Details",
  "description": "Order details header and actions for My Account",
  "type": "object",
  "properties": {
    "orderNumberPrefix": {
      "type": "string",
      "title": "Order Number Prefix",
      "default": "Order #"
    },
    "goBackLabel": {
      "type": "string",
      "title": "Go Back Label",
      "default": "Go back"
    },
    "cancelOrderLabel": {
      "type": "string",
      "title": "Cancel Order Label",
      "default": "Cancel order"
    },
    "reorderLabel": {
      "type": "string",
      "title": "Reorder Label",
      "default": "Reorder"
    },
    "notNowLabel": {
      "type": "string",
      "title": "Not Now Label",
      "default": "Not now"
    },
    "cancelConfirmMessage": {
      "type": "string",
      "title": "Cancel Confirm Message",
      "default": "Are you sure you want to cancel this order?"
    },
    "cancelSuccessToast": {
      "type": "string",
      "title": "Cancel Success Toast",
      "default": "Order cancelled successfully"
    },
    "cancelErrorToast": {
      "type": "string",
      "title": "Cancel Error Toast",
      "default": "Failed to cancel order"
    },
    "approveLabel": {
      "type": "string",
      "title": "Approve Label",
      "default": "Approve"
    },
    "rejectLabel": {
      "type": "string",
      "title": "Reject Label",
      "default": "Reject"
    },
    "rejectModalTitle": {
      "type": "string",
      "title": "Reject Modal Title",
      "default": "Reject approval request"
    },
    "rejectModalMessage": {
      "type": "string",
      "title": "Reject Modal Message",
      "default": "You're about to reject this approval request, triggered by the {policy} policy. Rejecting any approval request will deny the entire order.\n\nThis action is permanent and cannot be undone."
    },
    "rejectModalConfirmText": {
      "type": "string",
      "title": "Reject Modal Confirm Text",
      "default": "Reject"
    },
    "approveSuccessToast": {
      "type": "string",
      "title": "Approve Success Toast",
      "default": "{policy} policy approved successfully."
    },
    "approveErrorToast": {
      "type": "string",
      "title": "Approve Error Toast",
      "default": "Policy couldn't be approved due to a technical issue."
    },
    "rejectSuccessToast": {
      "type": "string",
      "title": "Reject Success Toast",
      "default": "{policy} policy rejected successfully. Order denied."
    },
    "rejectErrorToast": {
      "type": "string",
      "title": "Reject Error Toast",
      "default": "Policy couldn't be rejected due to a technical issue."
    }
  },
  "readOnly": false,
  "writeOnly": false,
  "deprecated": false,
  "$abstract": false
}
