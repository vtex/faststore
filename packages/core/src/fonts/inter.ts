// Default stub — safe for Babel and stores that do not enable optimizedFonts.
// When experimental.optimizedFonts is true in discovery.config, next.config.js
// redirects this module to inter.optimized.ts (which imports next/font/google
// and therefore requires SWC).
export const inter = null
