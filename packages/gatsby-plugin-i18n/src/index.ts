// import { createContext, useContext } from 'react'

export * from './helpers'

// const LocalizationContext = createContext<any>({ defaultLocale: 'en', locale: 'en' })

// export const useLocalizationContext = () => useContext(LocalizationContext)
// export { useLocalizationContext } from './components/LocalizationContextProvider'
