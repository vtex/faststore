import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'

import { SelectField } from '@faststore/components'

<Meta title="Molecules/SelectField" component={SelectField} argTypes={{}} />

export const Template = (args) => (
  <SelectField id="select-field" label="SelectField" />
)

<header>

# SelectField

SelectField is composed of a Select input and its corresponding Label. It allows users to select one option from a set.

</header>

## Usage

```jsx
import SelectField from '@faststore/ui'
// Styles
import '@faststore/ui/src/components/molecules/SelectField/styles.scss'
```

<Canvas>
  <Story
    name="default"
    args={{
      id: 'select-default',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
