import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'

import { Tag } from '@faststore/components'

<Meta title="Molecules/Tag" component={Tag} argTypes={{}} />

export const Template = (args) => <Tag label="CheckboxField" />

<header>

# Tag

CheckboxField is composed of a Checkbox input and its corresponding Label. It allows users to select one option from a set.

</header>

## Usage

```jsx
import { Tag } from '@faststore/ui'
// Styles
import '@faststore/ui/src/components/molecules/Tag/styles.scss'
```

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="default" />
