---
hide_table_of_contents: true
sidebar_position: 1
---

import PropsComponent from "@site/src/components/PropsComponent/PropsComponent";

# `product`

Returns the details of a product based on the specified locator.

<div className="grid grid-cols-2 gap-20">

<div>

## Usage

### Arguments

<PropsComponent
      name="locator"
      type="IStoreSelectedFacet"
      description="An array of selected search facets."
      required="true"
    />

### Response

<PropsComponent
      name="StoreProduct"
      type="StoreProduct"
      description="Product information."
    />

### Fields

Below you can see a list of all fields that can be queried with `product`. Click on the links to learn more details about each field.

<ul>

</ul>

</div>
<div>

**Query**

```graphql
query Product($locator: [IStoreSelectedFacet!]!) {
  product(locator: $locator) {
    # StoreProduct fields
  }
}
```

**Example**

Getting the name, description, brand name and image URL and alternate name of a product with SKU of `123`.

```graphql
query {
  product (locator:[{key:"id", value:"123"}]) {
    name,
    description,
    brand {
      name
    },
    image{
      url
    }
  }
}
```

</div>
</div>
