---
description: This component is used to announce to users that the product is out of stock.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/OutOfStock___5863b0eb4a4a8ce9a98da5c760461e61.png
---

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import {
  Alert,
  CartSidebar,
  CartSidebarList,
  CartSidebarFooter,
  UIProvider,
  CartItem,
  CartItemImage,
  CartItemSummary,
  Gift,
  GiftContent,
  GiftImage,
  OrderSummary,
} from '@faststore/ui'

import { TagIcon, Truck } from '@faststore/ui'
import { product } from 'site/mocks/product'
import { useFormattedPrice } from 'site/components/utilities/usePriceFormatter'

import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const outOfStockPath = path.resolve(__filename)
  const components = ['OutOfStock.tsx']
  const [outOfStockProps] = getComponentPropsFrom(outOfStockPath, components)
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        outOfStockProps,
      },
    },
  }
}

export const OutOfStockPropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { outOfStockProps } = useSSG()
  return {
    OutOfStock: <PropsSection propsList={outOfStockProps} />,
  }[component]
}

export const skuActiveVariants =
  product.isVariantOf.skuVariants.activeVariations
export const activeVariations = Object.keys(skuActiveVariants).map((key) => ({
  label: key,
  option: skuActiveVariants[key],
}))

<header>

# Cart Sidebar

The `CartSidebar` displays the summary of items added to the cart along with detailed information, and checkout option.

</header>

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import { CartSidebar, CartSidebarList, CartSidebarFooter } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/organisms/CartSidebar/styles.scss'
```

---

## Usage

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection containerStyle={{display: 'block'}}>
      <UIProvider>
      <CartSidebar totalItems={0} alertIcon={<Truck />} alertText="Free shipping starts at $300" >
        <CartSidebarList>
          <CartItem
            price={{
              value: product.offers.offers[0].price,
              listPrice: product.offers.offers[0].listPrice,
              formatter: useFormattedPrice,
            }}
            quantity={3}
          >
          <CartItemImage>
            <img
              data-fs-image
              src={product.image[0].url}
              alt={product.image[0].alternateName}
            />
          </CartItemImage>
          <CartItemSummary
            title={product.isVariantOf.name}
            activeVariations={activeVariations}
          />
          </CartItem>
          <Gift icon={<TagIcon />}>
            <GiftImage>
              <img
                src="https://storeframework.vtexassets.com/arquivos/ids/190902/unsplash-magic-mouse.jpg?v=637800136963870000"
                alt="Magicwhite"
                width={89}
                height={89}
              />
            </GiftImage>
            <GiftContent
              productName="Apple Magic Mouse"
              price={{ value: 999, listPrice: 999, formatter: useFormattedPrice }}
            />
          </Gift>
        </CartSidebarList>
        <CartSidebarFooter>
          <OrderSummary
            subtotalLabel="Subtotal (3 products)"
            subtotalValue={useFormattedPrice(3)}
            discountValue={`-${useFormattedPrice(100)}`}
            totalValue={useFormattedPrice(12300)}
          />
        </CartSidebarFooter>
      </CartSidebar>
      </UIProvider>
    </OverviewSection>

  </Tab>
  <Tab>
    ```tsx
    <OutOfStock
        title="Out of Stock"
        subtitle="Notify me when available"
        inputLabel="Email"
        buttonLabel="Notify Me"
        onSubmit={handleSubmit}
        disabled={isLoading}
      />
    ```

  </Tab>
</Tabs>

---

## Props

### Out of Stock

<OutOfStockPropsSection component="OutOfStock" />

---

## Design Tokens

### Nested Elements

#### Title

<TokenTable>
  <TokenRow
    token="--fs-out-of-stock-title-margin-bottom"
    value="var(--fs-spacing-0)"
  />
  <TokenRow token="--fs-out-of-stock-title-line-height" value="1.15" />
  <TokenRow
    token="--fs-out-of-stock-title-size"
    value="var(--fs-text-size-lead)"
  />
  <TokenRow
    token="--fs-out-of-stock-title-weight"
    value="var(--fs-text-weight-bold)"
  />
  <TokenRow
    token="--fs-out-of-stock-title-color"
    value="var(--fs-color-neutral-text)"
    isColor
  />
</TokenTable>

#### Message

<TokenTable>
  <TokenRow
    token="--fs-out-of-stock-message-column-gap"
    value="var(--fs-spacing-0)"
  />
  <TokenRow
    token="--fs-out-of-stock-message-margin-bottom"
    value="var(--fs-spacing-3)"
  />
  <TokenRow token="--fs-out-of-stock-message-line-height" value="1.15" />
  <TokenRow
    token="--fs-out-of-stock-message-size"
    value="var(--fs-text-size-base)"
  />
  <TokenRow
    token="--fs-out-of-stock-message-weight"
    value="var(--fs-text-weight-regular)"
  />
  <TokenRow
    token="--fs-out-of-stock-message-color"
    value="var(--fs-color-success-text)"
    isColor
  />
</TokenTable>

#### Button

<TokenTable>
  <TokenRow token="--fs-out-of-stock-button-width" value="100%" />
  <TokenRow
    token="--fs-out-of-stock-button-margin-top"
    value="var(--fs-spacing-3)"
  />
</TokenTable>

---

## Customization

`data-fs-out-of-stock`

`data-fs-out-of-stock-title`

`data-fs-out-of-stock-message`

`data-fs-out-of-stock-button`
