---
description: Icons are simplified graphics used to represent concepts and help users navigate the UI.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Icon___9437da8db13517090534a38f4870d702.png
---

import PropsSection from 'site/components/PropsSection'
import { OverviewSection } from 'site/components/OverviewSection'
import { Icon } from '@faststore/ui'

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const iconPath = path.resolve(__filename)
  const components = ['Icon.tsx']
  const [iconProps] = getComponentPropsFrom(iconPath, components)
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        iconProps,
      },
    },
  }
}

export const IconPropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { iconProps } = useSSG()
  return {
    Icon: <PropsSection propsList={iconProps} />,
  }[component]
}

<header>

# Icon

Icons are simplified graphics used to represent concepts and help users navigate the UI.

This project has a [set of icons](../../docs/icons) that uses SVGs from [Phosphor icons](https://phosphoricons.com).

</header>

## Overview

<OverviewSection>
  <Icon name="ShoppingCart" weight="thin" width={32} height={32} />
  <Icon name="ShoppingCart" weight="light" />
  <Icon name="ShoppingCart" weight="regular" width={16} height={16} />
  <Icon name="ShoppingCart" weight="bold" width={20} height={20} />
</OverviewSection>

```tsx
  <Icon name="ShoppingCart" weight="thin" width={32} height={32} />
  <Icon name="ShoppingCart" weight="light" />
  <Icon name="ShoppingCart" weight="regular" width={16} height={16} />
  <Icon name="ShoppingCart" weight="bold" width={20} height={20} />
```

**note**: default values for `width` and `height` is 24.

---

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import { Icon } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/atoms/Icon/styles.scss'
```

---

## Usage

<OverviewSection>
  <Icon name="ShoppingCart" />
</OverviewSection>
```tsx
<Icon name="ShoppingCart" />
```

---

## Props

<IconPropsSection component="Icon" />

List of available [icons](../../docs/icons)

## Customization

For further customization, you can use the following data attributes:

`data-fs-icon`
