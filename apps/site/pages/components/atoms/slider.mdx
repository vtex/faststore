---
description: Sliders allow users to select a single value from a range.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Slider___9fa1f5d802a9bbdc93a77db318594af7.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import { SectionItem, SectionList } from 'site/components/SectionItem'
import { Slider, Price, PriceRange } from '@faststore/ui'
import PriceRangeUsage from 'site/components/PriceRange/PriceRangeUsage'
import { useFormattedPrice } from 'site/components/utilities/usePriceFormatter'

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const sliderPath = path.resolve(__filename)
  const components = ['Slider.tsx']
  const [sliderProps] = getComponentPropsFrom(sliderPath, components)
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        sliderProps,
      },
    },
  }
}

export const SliderPropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { sliderProps } = useSSG()
  return {
    Slider: <PropsSection propsList={sliderProps} />,
  }[component]
}

<header>

# Slider

Sliders allow users to select a single value from a range.

</header>

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <p>Default Version</p>
    <OverviewSection>
      <Slider
        max={{ absolute: 500, selected: 250 }}
        min={{ absolute: 0, selected: 100 }}
        absoluteValuesLabel={{ min: "0", max: "500"}}
      />
    </OverviewSection>
    <p>Show Labels on Hover</p>
    <OverviewSection>
      <Slider
        max={{ absolute: 500, selected: 250 }}
        min={{ absolute: 0, selected: 100 }}
        absoluteValuesLabel={{ min: "0", max: "500"}}
        maxValueLabelComponent={(maxValue) => {return <span>{maxValue}</span>}}
        minValueLabelComponent={(minValue) => {return <span>{minValue}</span>}}
      />
    </OverviewSection>

  </Tab>
  <Tab>
    <p>Default Version</p>
    ```tsx
    <Slider
      max={{ absolute: 500, selected: 250 }}
      min={{ absolute: 0, selected: 100 }}
      absoluteValuesLabel={{ min: "0", max: "500"}}
    />
    ```
    <p>Show Labels on Hover</p>
    ```tsx
    <Slider
      max={{ absolute: 500, selected: 250 }}
      min={{ absolute: 0, selected: 100 }}
      absoluteValuesLabel={{ min: "0", max: "500"}}
      maxValueLabelComponent={(maxValue) => {return <span>{maxValue}</span>}}
      minValueLabelComponent={(minValue) => {return <span>{minValue}</span>}}
    />
    ```
  </Tab>
</Tabs>

---

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import { Slider } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/atoms/Slider/styles.scss'
```

---

## Usage

<OverviewSection>
  <Slider
    max={{ absolute: 500, selected: 250 }}
    min={{ absolute: 0, selected: 100 }}
    absoluteValuesLabel={{
      min: useFormattedPrice(0),
      max: useFormattedPrice(500),
    }}
    maxValueLabelComponent={(maxValue) => {
      return <Price formatter={useFormattedPrice} value={maxValue} />
    }}
    minValueLabelComponent={(minValue) => {
      return <Price formatter={useFormattedPrice} value={minValue} />
    }}
  />
</OverviewSection>
```tsx
<Slider
  max={{ absolute: 500, selected: 250 }}
  min={{ absolute: 0, selected: 100 }}
  absoluteValuesLabel={{
    min: useFormattedPrice(0),
    max: useFormattedPrice(500),
  }}
  maxValueLabelComponent={(maxValue) => {
    return <Price formatter={useFormattedPrice} value={maxValue} />
  }}
  minValueLabelComponent={(minValue) => {
    return <Price formatter={useFormattedPrice} value={minValue} />
  }}
/>
```

---

## Props

<SliderPropsSection component="Slider" />

### Range

export const propsRange = [
  {
    name: 'absolute',
    type: 'number',
    description: 'The absolute value of the slider.',
  },
  {
    name: 'selected',
    type: 'number',
    description:
      'The selected value of the slider.',
  }
]

<PropsSection propsList={propsRange} />

### RangeLabel

export const propsRangeLabel = [
  {
    name: 'min',
    type: 'string | ReactNode',
    description: 'Label for minimum range value.',
  },
  {
    name: 'max',
    type: 'string | ReactNode',
    description:
      'Label for maximum range value.',
  }
]

<PropsSection propsList={propsRangeLabel} />

---

## Design Tokens

<TokenTable>
  <TokenRow token="--fs-slider-height" value="var(--fs-spacing-2)" />
  <TokenRow
    token="--fs-slider-border-radius"
    value="var(--fs-border-radius-pill)"
  />
  <TokenRow token="--fs-slider-margin-bottom" value="var(--fs-spacing-3)" />
  <TokenDivider />
  <TokenRow
    token="--fs-slider-slider-bkg-color"
    value="var(--fs-color-neutral-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-slider-slider-selection-bkg-color"
    value="var(--fs-color-primary-bkg-light-active)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-slider-transition-function"
    value="var(--fs-transition-function)"
  />
  <TokenRow
    token="--fs-slider-transition-property"
    value="var(--fs-transition-property)"
  />
  <TokenRow
    token="--fs-slider-transition-timing"
    value="var(--fs-transition-timing)"
  />
</TokenTable>

### Nested Elements

#### Thumb

<TokenTable>
  <TokenRow token="--fs-slider-thumb-size" value="var(--fs-spacing-4)" />
  <TokenRow
    token="--fs-slider-thumb-bkg-color"
    value="var(--fs-color-primary-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-slider-thumb-bkg-color-hover"
    value="var(--fs-color-primary-bkg-hover)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-slider-thumb-border-width"
    value="var(--fs-border-width)"
  />
  <TokenRow
    token="--fs-slider-thumb-border-radius"
    value="var(--fs-border-radius-circle)"
  />
  <TokenRow
    token="--fs-slider-thumb-border-color"
    value="var(--fs-slider-thumb-bkg-color)"
    isColor
  />
  <TokenRow
    token="--fs-slider-thumb-border-color-hover"
    value="var(--fs-slider-thumb-bkg-color-hover)"
    isColor
  />
</TokenTable>

#### Absolute Values

<TokenTable>
  <TokenRow
    token="--fs-slider-absolute-values-text-color"
    value="var(--fs-color-disabled-text)"
    isColor
  />
</TokenTable>

#### Value Label

<TokenTable>
  <TokenRow
    token="--fs-slider-value-label-bottom"
    value="var(--fs-spacing-3)"
  />
  <TokenRow
    token="--fs-slider-value-label-bkg-color"
    value="var(--fs-color-body-bkg)"
    isColor
  />
</TokenTable>

---

## Customization

For further customization, you can use the following data attributes:

`data-fs-slider`

`data-fs-slider-absolute-values`

`data-fs-slider-range`

`data-fs-slider-thumb="left" | "right"`

`data-fs-slider-value-label="min" | "max"`

---

## Related components

<SectionList>
  <SectionItem
    title="PriceRange"
    description="
Price Ranges are sliders that allows users to select a maximum and minimum price."
    actionPath="/components/organisms/price-range"
  >
    <PriceRangeUsage />
  </SectionItem>
</SectionList>
