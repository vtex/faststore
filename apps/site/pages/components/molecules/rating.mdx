---
description: Rating displays a product rating based on other shoppers' feedback.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/AggregateRating___a8300916874f5a11de7633b7cabc12e9.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import { Rating, Icon } from '@faststore/ui'
import { Star, Heart } from '@faststore/components'
import RatingActionable from 'site/components/Rating/RatingActionable'

<header>

# Rating

The `Rating` component displays the average rating of a product based on other shoppers' feedback and reviews. `Rating` supports icons from our components.

</header>

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <h4>Readonly</h4>
      <Rating value={3.5} icon={<Star />} />
    </OverviewSection>
    <OverviewSection>
      <h4>Actionable</h4>
      <RatingActionable value={0} />
    </OverviewSection>
  </Tab>
  <Tab>

    ```tsx
    <Rating value={3.5} icon={<Star />} />
    ```

    ```tsx
    const [rating, setRating] = useState(0)
    <Rating value={rating} onChange={setRating} icon={<Star />} />
    ```

  </Tab>
</Tabs>

---

## Import

Import the component from <a href="https://www.faststore.dev/reference/ui/get-started-faststore-ui">@faststore/ui</a>

```tsx
import { Rating } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/molecules/Rating/styles.scss'
```

---

## Usage

<OverviewSection>
  <Rating value={3.5} icon={<Star />} />
</OverviewSection>
```tsx live
<Rating value={3.5} icon={<Star />} />
```

---

## Props

<PropsSection name="Rating" />

---

## Design Tokens

<TokenTable>
  <TokenRow token="--fs-rating-gap" value="var(--fs-spacing-3)" />
  <TokenRow token="--fs-rating-color" value="var(--fs-color-text)" isColor />
</TokenTable>

### Nested Elements

#### Icon

<TokenTable>
  <TokenRow token="--fs-rating-icon-width" value="var(--fs-spacing-3)" />
  <TokenRow
    token="--fs-rating-icon-height"
    value="var(--fs-rating-icon-width)"
  />
</TokenTable>

### Variants

#### Readonly

<OverviewSection>
  <Rating value={3.5} icon={<Star />} />
</OverviewSection>
```tsx live
<Rating value={3.5} icon={<Star />} />
```

#### Other Icon

<OverviewSection>
  <Rating value={3.5} icon={<Heart />} />
</OverviewSection>
```tsx live
<Rating value={3.8} icon={<Heart />} />
```

#### Actionable

<OverviewSection>
  <RatingActionable value={3} />
</OverviewSection>

```tsx live
const [rating, setRating] = useState(3)
<Rating value={rating} onChange={setRating} icon={<Star />} />
```

<TokenTable>
  <TokenRow token="--fs-rating-actionable-gap" value="0" />
</TokenTable>
