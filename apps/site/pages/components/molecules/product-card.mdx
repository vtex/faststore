---
description: ProductCard displays summarized information about a product.
keywords:
  - UI library
  - ecommerce
  - react
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/ProductCardComponent___e6502d57cf7e5bf247f86b64577d4141.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { SectionItem, SectionList } from 'site/components/SectionItem'
import { OverviewSection } from 'site/components/OverviewSection'
import {
  ProductShelf,
  ProductShelfItems,
  ProductShelfItem,
  ProductCard,
  ProductCardContent,
  ProductCardImage,
  ProductGrid,
  ProductGridItem,
} from '@faststore/ui'
import { useFormattedPrice } from 'site/components/utilities/usePriceFormatter'

import { product } from 'site/mocks/product'
import { products } from 'site/mocks/products'
import { propsPriceDefinition } from 'site/components/PriceDefinitionProps/PriceDefinitionProps'

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const productCardPath = path.resolve(__filename)
  const components = [
    'ProductCard.tsx',
    'ProductCardImage.tsx',
    'ProductCardContent.tsx',
  ]
  const [productCardProps, productCardImageProps, productCardContentProps] =
    getComponentPropsFrom(productCardPath, components)
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        productCardProps,
        productCardImageProps,
        productCardContentProps,
      },
    },
  }
}

export const ProductCardPropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { productCardProps, productCardImageProps, productCardContentProps } =
    useSSG()
  return {
    ProductCard: <PropsSection propsList={productCardProps} />,
    ProductCardImage: <PropsSection propsList={productCardImageProps} />,
    ProductCardContent: <PropsSection propsList={productCardContentProps} />,
  }[component]
}

<header>

# Product Card

Displays summarized information about a product. They usually present a call-to-action button, as well as the product's name, price, and image.

</header>

The `ProductCard` is a compound of the following:

- `ProductCardImage`: wraps the product's image.
- `ProductCardContent`: wraps the content's details.

---

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr' }}
    >
      <ProductCard>
        <ProductCardImage>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          ratingValue={3.5}
          onButtonClick={() => {}}
        />
      </ProductCard>
      <ProductCard bordered>
        <ProductCardImage aspectRatio={3 / 4}>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          showDiscountBadge
        />
      </ProductCard>
      <ProductCard variant="wide">
        <ProductCardImage aspectRatio={3 / 2}>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          ratingValue={3.5}
          showDiscountBadge
        />
      </ProductCard>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductCard>
      <ProductCardImage>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        ratingValue={3.5}
        onButtonClick={() => {}}
      />
    </ProductCard>
    ```

    ```tsx
    <ProductCard bordered>
      <ProductCardImage aspectRatio={3 / 4}>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        showDiscountBadge
      />
    </ProductCard>
    ```

    ```tsx
    <ProductCard variant="wide">
      <ProductCardImage aspectRatio={3 / 2}>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        ratingValue={3.5}
        showDiscountBadge
      />
    </ProductCard>
    ```

  </Tab>
</Tabs>

---

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import {
  ProductCard,
  ProductCardImage,
  ProductCardContent,
} from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/molecules/ProductCard/styles.scss'
```

---

## Usage

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr' }}
    >
      <ProductCard>
        <ProductCardImage>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          ratingValue={3.5}
          onButtonClick={() => {}}
        />
      </ProductCard>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductCard>
      <ProductCardImage>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        ratingValue={3.5}
        onButtonClick={() => {}}
      />
    </ProductCard>
    ```
  </Tab>
</Tabs>

---

## Props

All product card related components support all attributes also supported by their respective HTML tags. Besides those attributes, the following props are also supported:

### Product Card

<ProductCardPropsSection component="ProductCard" />

### Product Card Image

<ProductCardPropsSection component="ProductCardImage" />

### Product Card Content

<ProductCardPropsSection component="ProductCardContent" />

### Other Resources

#### PriceDefinition

<PropsSection propsList={propsPriceDefinition} />

---

## Design Tokens

<TokenTable>
  <TokenRow
    token="--fs-product-card-padding"
    value="var(--fs-spacing-1) var(--fs-spacing-1) var(--fs-spacing-2) var(--fs-spacing-1)"
  />
  <TokenRow token="--fs-product-card-min-width" value="10rem" />
  <TokenDivider />
  <TokenRow token="--fs-product-card-shadow" value="var(--fs-shadow)" />
  <TokenRow
    token="--fs-product-card-shadow-hover"
    value="var(--fs-shadow-hover)"
  />
  <TokenDivider />
  <TokenRow
    token="--fs-product-card-bkg-color"
    value="var(--fs-color-body-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-product-card-bkg-color-hover"
    value="var(--fs-product-card-bkg-color)"
    globalValue="var(--fs-color-body-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-product-card-bkg-color-focus"
    value="var(--fs-product-card-bkg-color-hover)"
    globalValue="var(--fs-color-body-bkg)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-product-card-border-radius"
    value="var(--fs-border-radius)"
  />
  <TokenRow
    token="--fs-product-card-border-color-hover"
    value="var(--fs-border-color-hover)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-product-card-transition-function"
    value="var(--fs-transition-function)"
  />
  <TokenRow
    token="--fs-product-card-transition-property"
    value="var(--fs-transition-property)"
  />
  <TokenRow
    token="--fs-product-card-transition-timing"
    value="var(--fs-transition-timing)"
  />
</TokenTable>

### Nested Elements

#### Image

<TokenTable>
  <TokenRow
    token="--fs-product-card-img-radius"
    value="var(--fs-product-card-border-radius)"
  />
  <TokenRow token="--fs-product-card-img-scale-hover" value="1" />
</TokenTable>

#### Title

<TokenTable>
  <TokenRow
    token="--fs-product-card-title-color"
    value="var(--fs-color-text)"
    isColor
  />
  <TokenRow
    token="--fs-product-card-title-size"
    value="var(--fs-text-size-base)"
  />
  <TokenRow
    token="--fs-product-card-title-weight"
    value="var(--fs-text-weight-regular)"
  />
  <TokenRow
    token="--fs-product-card-title-max-lines"
    value="var(--fs-text-max-lines)"
  />
</TokenTable>

#### Price

<TokenTable>
  <TokenRow token="--fs-product-card-price-gap" value="var(--fs-spacing-1)" />
  <TokenDivider />
  <TokenRow
    token="--fs-product-card-price-color"
    value="var(--fs-color-text)"
    isColor
  />
  <TokenRow
    token="--fs-product-card-price-size"
    value="var(--fs-text-size-base)"
  />
</TokenTable>

### Variants

#### Default

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr' }}
    >
      <ProductCard>
        <ProductCardImage>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          ratingValue={3.5}
          onButtonClick={() => {}}
        />
      </ProductCard>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductCard>
      <ProductCardImage>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        ratingValue={3.5}
        onButtonClick={() => {}}
      />
    </ProductCard>
    ```
  </Tab>
</Tabs>

#### Out of Stock

To replicate this style, add `outOfStock` prop on both `ProductCard` and `ProductCardContent`.

export const outOfStock =
  product.offers.offers[0].availability === 'https://schema.org/InStock'

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr' }}
    >
      <ProductCard outOfStock={outOfStock}>
        <ProductCardImage>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          outOfStock={outOfStock}
        />
      </ProductCard>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    const outOfStock = product.offers.offers[0].availability !== 'https://schema.org/InStock'

    <ProductCard outOfStock={outOfStock}>
      <ProductCardImage>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        outOfStock={outOfStock}
      />
    </ProductCard>
    ```

  </Tab>
</Tabs>

<TokenTable>
  <TokenRow
    token="--fs-product-card-out-of-stock-bkg-color"
    value="var(--fs-color-disabled-bkg)"
    isColor
  />
  <TokenRow token="--fs-product-card-out-of-stock-img-opacity" value=".5" />
</TokenTable>

#### Bordered

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr' }}
    >
      <ProductCard bordered>
        <ProductCardImage>
          <img
            data-fs-image
            src={product.image[1].url}
            alt={product.image[1].alternateName}
          />
        </ProductCardImage>
        <ProductCardContent
          title={product.isVariantOf.name}
          price={{
            value: product.offers.offers[0].price,
            listPrice: product.offers.offers[0].listPrice,
            formatter: useFormattedPrice,
          }}
          ratingValue={3.5}
          onButtonClick={() => {}}
        />
      </ProductCard>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductCard bordered>
      <ProductCardImage>
        <img
          data-fs-image
          src={product.image[1].url}
          alt={product.image[1].alternateName}
        />
      </ProductCardImage>
      <ProductCardContent
        title={product.isVariantOf.name}
        price={{
          value: product.offers.offers[0].price,
          listPrice: product.offers.offers[0].listPrice,
          formatter: useFormattedPrice,
        }}
        ratingValue={3.5}
        onButtonClick={() => {}}
      />
    </ProductCard>
    ```
  </Tab>
</Tabs>

<TokenTable>
  <TokenRow
    token="--fs-product-card-border-width"
    value="var(--fs-border-width)"
  />
  <TokenRow
    token="--fs-product-card-border-color"
    value="var(--fs-border-color-light)"
    isColor
  />
</TokenTable>

#### Wide

Prefer using `buyButton` or `discountBadge`, never both.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
  containerStyle={{ display: 'grid', gridTemplateColumns: '2fr 2fr' }}
>
  <ProductCard variant="wide">
    <ProductCardImage aspectRatio={2 / 1}>
      <img
        data-fs-image
        src={product.image[1].url}
        alt={product.image[1].alternateName}
      />
    </ProductCardImage>
    <ProductCardContent
      title={product.isVariantOf.name}
      price={{
        value: product.offers.offers[0].price,
        listPrice: product.offers.offers[0].listPrice,
        formatter: useFormattedPrice,
      }}
      ratingValue={3.5}
      onButtonClick={() => {}}
    />
  </ProductCard>
  <ProductCard variant="wide">
    <ProductCardImage aspectRatio={2 / 1}>
      <img
        data-fs-image
        src={product.image[1].url}
        alt={product.image[1].alternateName}
      />
    </ProductCardImage>
    <ProductCardContent
      title={product.isVariantOf.name}
      price={{
        value: product.offers.offers[0].price,
        listPrice: product.offers.offers[0].listPrice,
        formatter: useFormattedPrice,
      }}
      ratingValue={3.5}
      showDiscountBadge
    />
  </ProductCard>
</OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductCard variant="wide">
    <ProductCardImage aspectRatio={2 / 1}>
      <img
        data-fs-image
        src={product.image[1].url}
        alt={product.image[1].alternateName}
      />
    </ProductCardImage>
    <ProductCardContent
      title={product.isVariantOf.name}
      price={{
        value: product.offers.offers[0].price,
        listPrice: product.offers.offers[0].listPrice,
        formatter: useFormattedPrice,
      }}
      ratingValue={3.5}
      onButtonClick={() => {}}
    />
  </ProductCard>
    ```

    ```tsx
    <ProductCard variant="wide">
    <ProductCardImage aspectRatio={2 / 1}>
      <img
        data-fs-image
        src={product.image[1].url}
        alt={product.image[1].alternateName}
      />
    </ProductCardImage>
    <ProductCardContent
      title={product.isVariantOf.name}
      price={{
        value: product.offers.offers[0].price,
        listPrice: product.offers.offers[0].listPrice,
        formatter: useFormattedPrice,
      }}
      ratingValue={3.5}
      showDiscountBadge
    />

  </ProductCard>
    ```
  </Tab>
</Tabs>

<TokenTable>
  <TokenRow token="--fs-product-card-wide-padding" value="0" />
  <TokenRow
    token="--fs-product-card-wide-content-padding"
    value="var(--fs-spacing-2)"
  />
  <TokenRow token="--fs-product-card-wide-min-width" value="9rem" />
  <TokenRow
    token="--fs-product-card-wide-bkg-color"
    value="var(--fs-color-neutral-bkg)"
    isColor
  />
</TokenTable>

---

## Use Cases

Use the `ProductCard` to:

- Present a product collection on the home page.
- Present product details on PLPs.
- Build a product slider that showcases multiple products at once, making it easier for visitors to browse your online store.

---

## Customization

`data-fs-product-card`

`data-fs-product-card-bordered="true"`

`data-fs-product-card-variant="default | wide"`

`data-fs-product-card-image`

`data-fs-product-card-content`

`data-fs-product-card-heading`

`data-fs-product-card-title`

`data-fs-product-card-prices`

`data-fs-product-card-actions`

---

## Related components

<SectionList>
  <SectionItem
    title="ProductShelf"
    description="
Displays a list of products to be used as a section on the store."
    actionPath="/components/organisms/product-shelf"
  >
    <ProductShelf>
      <ProductShelfItems>
        {products.map(({ product }, idx) => (
          <ProductShelfItem>
            <ProductCard>
              <ProductCardImage>
                <img
                  data-fs-image
                  src={product.image[0].url}
                  alt={product.image[0].alternateName}
                />
              </ProductCardImage>
              <ProductCardContent
                title={product.isVariantOf.name}
                price={{
                  value: product.offers.offers[0].price,
                  listPrice: product.offers.offers[0].listPrice,
                  formatter: useFormattedPrice,
                }}
              />
            </ProductCard>
          </ProductShelfItem>
        ))}
      </ProductShelfItems>
    </ProductShelf>
  </SectionItem>
  <SectionItem
    title="ProductGrid"
    description="
It's a section generally used on PLP pages to list the products available in the store."
    actionPath="/components/organisms/product-grid"
  >
    <ProductGrid>
      {products.map(({ product }, idx) => (
        <ProductGridItem>
          <ProductCard>
            <ProductCardImage>
              <img
                data-fs-image
                src={product.image[0].url}
                alt={product.image[0].alternateName}
              />
            </ProductCardImage>
            <ProductCardContent
              title={product.isVariantOf.name}
              price={{
                value: product.offers.offers[0].price,
                listPrice: product.offers.offers[0].listPrice,
                formatter: useFormattedPrice,
              }}
            />
          </ProductCard>
        </ProductGridItem>
      ))}
    </ProductGrid>
  </SectionItem>
</SectionList>
