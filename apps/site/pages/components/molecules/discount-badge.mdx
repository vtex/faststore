---
description: A custom `Badge` that display product's discounts.
keywords:
  - UI library
  - ecommerce
  - react
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Badge___bcedffeb307527c1606be224a7fb5ac1.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import { DiscountBadge } from '@faststore/ui'

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const discountBadgePath = path.resolve(__filename)
  const components = ['DiscountBadge.tsx']
  const [discountBadgeProps] = getComponentPropsFrom(
    discountBadgePath,
    components
  )
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        discountBadgeProps,
      },
    },
  }
}

export const DiscountBadgePropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { discountBadgeProps } = useSSG()
  return {
    DiscountBadge: <PropsSection propsList={discountBadgeProps} />,
  }[component]
}

<header>

# Discount Badge

A custom `Badge` that display product's discounts.

</header>

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <DiscountBadge listPrice={45} spotPrice={40} />
      <DiscountBadge listPrice={65} spotPrice={40} />
      <DiscountBadge listPrice={80} spotPrice={40} />
      <DiscountBadge listPrice={45} spotPrice={40} size="big" />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <DiscountBadge listPrice={45} spotPrice={40} />
    <DiscountBadge listPrice={65} spotPrice={40} />
    <DiscountBadge listPrice={80} spotPrice={40} />
    <DiscountBadge listPrice={45} spotPrice={40} size="big" />
    ```
  </Tab>
</Tabs>

---

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import { DiscountBadge } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/atoms/Badge/styles.scss'
import '@faststore/ui/src/components/molecules/DiscountBadge/styles.scss'
```

---

## Usage

<OverviewSection>
  <DiscountBadge listPrice={45} spotPrice={40} />
</OverviewSection>

```tsx
<DiscountBadge listPrice={45} spotPrice={40} />
```

---

## Props

<DiscountBadgePropsSection component="DiscountBadge" />

---

## Design Tokens

This component inherits [Badge](/components/atoms/badge) design tokens.

### Variants

#### Low Discount

<OverviewSection>
  <DiscountBadge listPrice={45} spotPrice={40} />
</OverviewSection>
```tsx
<DiscountBadge listPrice={45} spotPrice={40} />
```

<TokenTable>
  <TokenRow
    token="--fs-discount-badge-low-bkg-color"
    value="var(--fs-badge-success-bkg-color)"
    globalValue="var(--fs-color-success-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-low-text-color"
    value="var(--fs-badge-success-text-color)"
    globalValue="var(--fs-color-text)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-low-border-color"
    value="var(--fs-badge-success-border-color)"
    globalValue="var(--fs-color-success-bkg)"
    isColor
  />
</TokenTable>

#### Medium Discount

<OverviewSection>
  <DiscountBadge listPrice={65} spotPrice={40} />
</OverviewSection>

```tsx
<DiscountBadge listPrice={65} spotPrice={40} />
```

<TokenTable>
  <TokenRow
    token="--fs-discount-badge-medium-bkg-color"
    value="var(--fs-badge-warning-bkg-color)"
    globalValue="var(--fs-color-warning-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-medium-text-color"
    value="var(--fs-badge-warning-text-color)"
    globalValue="var(--fs-color-warning-text)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-medium-border-color"
    value="var(--fs-badge-warning-border-color)"
    globalValue="var(--fs-color-warning-bkg)"
    isColor
  />
</TokenTable>

#### High Discount

<OverviewSection>
  <DiscountBadge listPrice={80} spotPrice={40} />
</OverviewSection>
```tsx
<DiscountBadge listPrice={80} spotPrice={40} />
```

<TokenTable>
  <TokenRow
    token="--fs-discount-badge-high-bkg-color"
    value="var(--fs-badge-danger-bkg-color)"
    globalValue="var(--fs-color-danger-bkg)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-high-text-color"
    value="var(--fs-badge-danger-text-color)"
    globalValue="var(--fs-color-text)"
    isColor
  />
  <TokenRow
    token="--fs-discount-badge-high-border-color"
    value="var(--fs-badge-danger-border-color)"
    globalValue="var(--fs-color-danger-bkg)"
    isColor
  />
</TokenTable>

---

## Customization

For further customization, you can use the following data attributes:

`data-fs-discount-badge`

`data-fs-discount-badge-variant`

This component inherits [Badge](/components/atoms/badge) css selectors and styles.
