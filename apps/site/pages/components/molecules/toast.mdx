---
description: Toasts are used to display notifications. They appear temporarily, and should contain a single line of text directly related to the operation performed.
keywords:
  - UI library
  - ecommerce
  - react
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/PaymentMethods___b0f344b254360daa53406cd631887adf.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow, TokenDivider } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import { SectionItem, SectionList } from 'site/components/SectionItem'
import { Button, Toast, useUI, UIProvider } from '@faststore/ui'
import ToastUsage from 'site/components/Toast/ToastUsage'
import { CircleWavyCheck, CircleWavyWarning } from '@faststore/components'

<header>

# Toast

Toasts are used to display notifications. They appear temporarily, and should contain a single line of text directly related to the operation performed.

</header>

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection direction="row">
      <UIProvider>
        <b>Simple Toast</b>
        <ToastUsage message={'Simple Toast message.'} status={'INFO'} />
        <b>Info Toast</b>
        <ToastUsage title={'Hooray!'} message={'Thank for your Click.'} status={'INFO'} icon={<CircleWavyCheck size={30} />} />
      </UIProvider>
    </OverviewSection>
    <OverviewSection direction="row">
      <UIProvider>
        <b>Error Toast</b>
        <ToastUsage title={'Oops.'} message={'Something went wrong. Please Try again.'} status={'ERROR'} icon={<CircleWavyWarning size={30} />} />
        <b>Warning Toast</b>
        <ToastUsage title={'Warn.'} message={'Warning: Please Try again.'} status={'WARNING'} icon={<CircleWavyWarning size={30} />} />
      </UIProvider>
    </OverviewSection>
  </Tab>
  <Tab>
    <b>Simple Toast</b>

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  message: 'Simple Toast message.',
                  status: 'INFO',
                })
              }
              style={{ width: '16rem' }}
            >
              Click to trigger Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    <b>Info Toast</b>

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Hooray!',
                  message: 'Thank for your Click.',
                  status: 'INFO',
                  icon: <CircleWavyCheck size={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Click to trigger Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    <b>Error Toast</b>

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Oops.',
                  message: 'Something went wrong. Please Try again.',
                  status: 'ERROR',
                  icon: <CircleWavyWarning size={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Click to trigger Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    <b>Warning Toast</b>

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Oops.',
                  message: 'Warn: Please Try again.',
                  status: 'WARNING',
                  icon: <CircleWavyWarning size={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Click to trigger Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

  </Tab>
</Tabs>

---

## Import

Import the component from <a href="https://www.faststore.dev/reference/ui/get-started-faststore-ui">@faststore/ui</a>

```tsx
import { Toast } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/molecules/Toast/styles.scss'
```

---

## Usage

<OverviewSection direction="row">
  <UIProvider>
    <b>Info Toast</b>
    <ToastUsage title={'Hooray!'} message={'Thank for your Click.'} status={'INFO'} icon={<CircleWavyCheck size={30} />} />
  </UIProvider>
</OverviewSection>
```tsx
  const Toast = () => {
    const { toasts, pushToast } = useUI()
    return (
      <>
        <Button
          variant="primary"
          onClick={() =>
            pushToast({
              title: 'Hooray!',
              message: 'Thank for your Click.',
              status: 'INFO',
              icon: <CircleWavyCheck size={30} />,
            })
          }
          style={{ width: '16rem' }}
        >
          Click to trigger Toast
        </Button>
        {toasts.length > 0 && <Toast />}
      </>
    )
  }
```

---

## Props

### Toast

<PropsSection name="Toast" />

---

## Design Tokens

<TokenTable>
  <TokenRow
    token="--fs-toast-width"
    value="calc(100% - (2 * var(--fs-spacing-3)))"
  />
  <TokenRow token="--fs-toast-min-height" value="var(--fs-spacing-9)" />
  <TokenRow
    token="--fs-toast-padding"
    value="var(--fs-spacing-1) var(--fs-spacing-3) var(--fs-spacing-1) var(--fs-spacing-1)"
  />
  <TokenRow
    token="--fs-toast-margin"
    value="var(--fs-spacing-3) var(--fs-spacing-3) 0 var(--fs-spacing-3)"
  />
  <TokenDivider />
  <TokenRow token="--fs-toast-top-mobile" value="3.125rem" />
  <TokenRow token="--fs-toast-top-tablet" value="6.25rem" />
  <TokenDivider />
  <TokenRow
    token="--fs-toast-border-radius"
    value="var(--fs-border-radius-medium)"
  />
  <TokenRow token="--fs-toast-border-width" value="var(--fs-border-width)" />
  <TokenRow token="--fs-toast-border-color" value="transparent" isColor />
  <TokenDivider />
  <TokenRow token="--fs-toast-shadow" value="var(--fs-shadow-hover)" />
  <TokenRow
    token="--fs-toast-bkg-color"
    value="var(--fs-color-neutral-0)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-toast-transition-property"
    value="var(--fs-transition-property)"
  />
  <TokenRow
    token="--fs-toast-transition-timing"
    value="var(--fs-transition-timing)"
  />
  <TokenRow
    token="--fs-toast-transition-function"
    value="var(--fs-transition-function)"
  />
</TokenTable>

### Nested Elements

#### Icon

<TokenTable>
  <TokenRow
    token="--fs-toast-icon-container-min-width"
    value="var(--fs-spacing-7)"
  />
  <TokenRow
    token="--fs-toast-icon-container-height"
    value="var(--fs-toast-icon-container-min-width)"
  />
  <TokenRow
    token="--fs-toast-icon-container-bkg-color"
    value="var(--fs-color-primary-bkg-light)"
    isColor
  />
  <TokenRow
    token="--fs-toast-icon-container-border-radius"
    value="var(--fs-border-radius)"
  />
</TokenTable>

#### Title

<TokenTable>
  <TokenRow token="--fs-toast-title-size" value="var(--fs-text-size-body)" />
  <TokenRow
    token="--fs-toast-title-weight"
    value="var(--fs-text-weight-bold)"
  />
  <TokenRow token="--fs-toast-title-line-height" value="var(--fs-scale)" />
  <TokenRow token="--fs-toast-title-margin-left" value="var(--fs-spacing-3)" />
</TokenTable>

#### Message

<TokenTable>
  <TokenRow
    token="--fs-toast-message-size"
    value="var(--fs-toast-title-size)"
  />
  <TokenRow
    token="--fs-toast-message-line-height"
    value="var(--fs-toast-title-line-height)"
  />
  <TokenRow
    token="--fs-toast-message-margin-left"
    value="var(--fs-spacing-3)"
  />
</TokenTable>

---

## Customization

`data-fs-toast`

`data-fs-toast-visible="true | false"`

`data-fs-toast-icon-container`

`data-fs-toast-content`

`data-fs-toast-title`

`data-fs-toast-message`
