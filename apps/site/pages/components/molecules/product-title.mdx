---
description: The Product Title is commonly used on Product Details Page (PDP).
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Checkbox___5dfb3d931c093ee7ebfac8bc388e68b5.png
---

import { Tab, Tabs } from 'nextra-theme-docs'
import PropsSection from 'site/components/PropsSection'
import { TokenTable, TokenRow } from 'site/components/Tokens'
import { OverviewSection } from 'site/components/OverviewSection'
import { ProductTitle, DiscountBadge } from '@faststore/ui'

import path from 'path'
import { useSSG } from 'nextra/ssg'
import { getComponentPropsFrom } from 'site/components/utilities/propsSection'

export const getStaticProps = () => {
  const productTitlePath = path.resolve(__filename)
  const components = ['ProductTitle.tsx']
  const [productTitleProps] = getComponentPropsFrom(
    productTitlePath,
    components
  )
  return {
    props: {
      // We add an `ssg` field to the page props,
      // which will be provided to the Nextra `useSSG` hook.
      ssg: {
        productTitleProps,
      },
    },
  }
}

export const ProductTitlePropsSection = ({ component }) => {
  // Get the data from SSG, and render it as a component.
  const { productTitleProps } = useSSG()
  return {
    ProductTitle: <PropsSection propsList={productTitleProps} />,
  }[component]
}

<header>
# Product Title

The Product Title is commonly used on Product Details Page (PDP). It usually wraps the product name, a product label and the rating.

</header>

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <ProductTitle
        title={<h1>Apple Magic Mouse</h1>}
        label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
        refNumber="99995945"
        ratingValue={4}
      />
    </OverviewSection>
    <OverviewSection>
      <ProductTitle
        title={<h1>Apple Magic Mouse</h1>}
        label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
        ratingValue={4}
      />
    </OverviewSection>
    <OverviewSection>
      <ProductTitle
        title={<h1>Handmade Steel Pants Gorgeous</h1>}
        label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
      />
    </OverviewSection>
    <OverviewSection>
      <ProductTitle
        title={<h1>Handmade Steel Pants Gorgeous</h1>}
        label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
        ratingValue={4.5}
      />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <ProductTitle
      title={<h1>Apple Magic Mouse</h1>}
      label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
      refNumber="99995945"
      ratingValue={4}
    />
    ``` ```tsx
    <ProductTitle
      title={<h1>Apple Magic Mouse</h1>}
      label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
      ratingValue={4}
    />
    ``` ```tsx
    <ProductTitle
      title={<h1>Handmade Steel Pants Gorgeous</h1>}
      label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
    />
    ``` ```tsx
    <ProductTitle
      title={<h1>Handmade Steel Pants Gorgeous</h1>}
      label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
      ratingValue={4.5}
    />
    ```
  </Tab>
</Tabs>

---

## Import

Import the component from [@faststore/ui](/../components)

```tsx
import { ProductTitle } from '@faststore/ui'
```

Import Styles

```tsx
import '@faststore/ui/src/components/molecules/ProductTitle/styles.scss'
```

---

## Usage

<OverviewSection>
  <ProductTitle
    title={<h1>Apple Magic Mouse</h1>}
    label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
    refNumber="99995945"
    ratingValue={4.5}
  />
</OverviewSection>
```tsx
<ProductTitle
  title={<h1>Apple Magic Mouse</h1>}
  label={<DiscountBadge size="big" listPrice={100} spotPrice={90} />}
  refNumber="99995945"
  ratingValue={4.5}
/>
```

---

## Props

<ProductTitlePropsSection component="ProductTitle" />

---

## Design Tokens

<TokenTable>
  <TokenRow
    token="--fs-product-title-text-size"
    value="var(--fs-text-size-title-product)"
  />
  <TokenRow
    token="--fs-product-title-text-weight"
    value="var(--fs-text-weight-regular)"
  />
  <TokenRow token="--fs-product-title-line-height" value="1.12" />
  <TokenRow token="--fs-product-title-column-gap" value="var(--fs-spacing-2)" />
  <TokenRow token="--fs-product-title-row-gap" value="var(--fs-spacing-3)" />
</TokenTable>

---

## Nested Elements

### Addendum

<TokenTable>
  <TokenRow
    token="--fs-product-title-addendum-color"
    value="var(--fs-color-text-light)"
    isColor
  />
  <TokenRow
    token="--fs-product-title-addendum-size"
    value="var(--fs-text-size-1)"
  />
  <TokenRow token="--fs-product-title-addendum-line-height" value="1.7" />
</TokenTable>

---

## Customization

For further customization, you can use the following data attributes:

`data-fs-product-title`

`data-fs-product-title-header`

`data-fs-product-title-addendum`
