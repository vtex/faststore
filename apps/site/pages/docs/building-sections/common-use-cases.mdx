---
title: 'Overrides - Use cases'
---

# Overrides - use cases


Now that you know how to override a component, checkout in this guide 
use cases to extend your component customization using overrides.
For the following use cases, we'll use the `Alert` component.

## Common use cases

### Simple Alert

In this example, the `Alert` component has only one prop changed, the `Icon`

![image-example](/)

To change this prop in the `Alert` component, we created the `SimpleAlert.tsx` file

```tsx filename="SimpleAlert.tsx" copy


```



## Advanced use cases