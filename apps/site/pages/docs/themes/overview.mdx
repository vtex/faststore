---
title: ' Overview'
sidebar_label: 'Themes'
---
import { Callout, Card, Cards } from 'nextra-theme-docs'

<header className="hero">

# Themes


</header>

By default, every FastStore project comes with the [Brandless theme](/docs/theming-overview#brandless-and-the-theming-structure). However, you have the option to use one of the [available themes](#available-themes) or [create a custom theme](#creating-a-custom-theme).


<Callout type="info" emoji="ℹ️">
  You have the flexibility and freedom to style the theme by overriding it using global or local tokens, or by starting from scratch using your preferred styling tool.
</Callout>

---

## Available Themes


<Cards num={2}>
  <Card image arrow title="Midnight" href="/docs/themes/midnight">
    <>![Midnight](https://vtexhelp.vtexassets.com/assets/docs/src/midnight___197d3bec531d5cbd4959f2cddabacc46.png)</>
  </Card>
  <Card image arrow title="Soft Blue" href="/docs/themes/soft-blue">
    <>![Soft Blue](https://vtexhelp.vtexassets.com/assets/docs/src/soft-blue___1d1970f5ce77ffb61bc8a10be86f69e7.png)</>
  </Card>
</Cards>


---

## Creating a Custom Theme

1. Create a new `.scss` file on `src/themes` (e.g. `my-theme.scss`) in your [starter](https://github.com/vtex-sites/starter.store), then add the tokens you want to overwrite inside the `.theme` class.

<Callout type="info" emoji="ℹ️">
 If you want, you can work on `custom-theme.scss` file that's ready to use. We are using [CSS cascade layers](https://developer.mozilla.org/en-US/docs/Learn/CSS/Building_blocks/Cascade_layers) feature to better deal with styling specificities issues, so keeping the `@layer theme` in the file is recommended.
</Callout>

<br></br>

```scss filename="src/themes/my-theme.scss" copy

@layer theme {
  .theme {
    // --------------------------------------------------------
    // Colors (Branding Core)
    // --------------------------------------------------------

    // --------------------------------------------------------
    // Typography (Branding Core)
    // --------------------------------------------------------

    // --------------------------------------------------------
    // Spacing (UI Essentials)
    // --------------------------------------------------------

    // --------------------------------------------------------
    // Grid & Layout (UI Essentials)
    // --------------------------------------------------------

    // --------------------------------------------------------
    // Interactive Controls (UI Essentials)
    // --------------------------------------------------------

    // --------------------------------------------------------
    // Refinements
    // --------------------------------------------------------

    // --------------------------------------------------------
    // FS UI Components
    // --------------------------------------------------------
    // Add here the customizations for FastStore UI components.
  }
}
```
<br></br>

2. Go to `faststore.config.js` and changes the `theme` from `custom-theme` to your file's name:

```js filename="faststore.config.js"
theme: 'my-theme',
```
---

 ## Related resources

 - [Theme overview](/docs/theming-overview)
 - [Global Tokens](/docs/global-tokens/overview)
 - [Making the first change in your store frontend](/docs/getting-started/2-setting-up-the-project#step-3-making-the-first-change-in-your-store-frontend)
