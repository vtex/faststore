{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["OCLIF_COMPILATION", "NODE_ENV"],
  "pipeline": {
    "site#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "docs#build": {
      "dependsOn": ["^build"],
      "outputs": [".docusaurus/**", "build/**"]
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "start": {},
    "size": {},
    "dev": {
      "cache": false
    }
  }
}
