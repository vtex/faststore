{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["OCLIF_COMPILATION", "NODE_ENV"],
  "pipeline": {
    "site#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "start": {},
    "size": {},
    "develop": {
      "cache": false
    },
    "dev": {
      "cache": false
    }
  }
}
